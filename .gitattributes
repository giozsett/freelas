# Normaliza fins de linha para evitar diffs falsos entre devs com
# core.autocrlf diferente (Windows/macOS/Linux).
* text=auto eol=lf

# Scripts do Windows precisam de CRLF para rodar corretamente.
*.ps1 text eol=crlf
*.bat text eol=crlf
*.cmd text eol=crlf
